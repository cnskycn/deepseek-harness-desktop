# DeepSeek Harness Desktop v1.4.5

基于 Electron 封装 dsh 运行时，内置 Node.js，双击即可使用。

**本版本内置 `@deepseek-ai/dsh@v0.1.5-rc.1`**。这是一个**重要修复版**，请所有 1.4.1–1.4.4 用户升级。

---

## 🔧 修复：启动后窗口黑屏 / 首次打不开需重启（严重）

**症状**（1.4.1–1.4.4）
- 首次打开卡在启动页、窗口一片黑，关闭后**第二次打开才能正常**
- 1.4.4 因内置 dsh 启动更慢，**几乎每次都无法打开**
- 但 dsh 服务本身是好的（浏览器可访问 `127.0.0.1:3080`）

**根因**
dsh 自 0.1.2 起对 Web 界面启用了**一次性 token 鉴权**：启动时会打印 `http://127.0.0.1:3080/?token=xxx`，未携带 token 的请求一律返回 **401**。

而旧版本的启动等待逻辑有两个缺陷叠加：
1. 健康探测 `checkHttp` **只判断"有没有响应"，不看状态码** —— 401 也被当成"服务就绪"
2. 等待逻辑设了 **15 秒宽限**：若 dsh 未在 15 秒内打印出带 token 的地址，就回退去探测**不带 token 的默认地址**

实测 dsh 首次启动（需重建 profile）到打印 token 地址耗时 **43 秒**，远超 15 秒 → 必然触发回退 → 用 401 的地址加载窗口 → **黑屏**。
第二次启动时 profile 已就绪、token 很快出现，所以"第二次能开"。

**修复内容**
- ✅ 健康探测改为**只接受 2xx/3xx**，401/403 不再视为就绪（从根上杜绝加载鉴权失败页）
- ✅ 未收到带 token 地址时，宽限期从 15 秒延长到 **90 秒**（覆盖首次初始化耗时）
- ✅ 若 3080 上存在**不可用的残留服务**（要求鉴权但我们拿不到 token），改为**自动清理后重启 dsh**，不再弹出用户看不懂、还挡在黑屏窗口后面的对话框

## 🩺 新增：启动日志与更清晰的等待反馈

- **启动日志落盘**：`%APPDATA%\DeepSeek Harness\logs\boot.log`
  记录 Node 选择、端口检测、dsh 地址解析、就绪判定与界面加载全流程，便于排查此类"卡启动"问题
- **启动页显示已等待秒数**，并在 60 秒 / 150 秒时给出针对性提示（首次初始化慢属正常）
- **修复错误页失效**：其 CSP 未允许内联脚本，导致「重试」按钮和日志显示**一直被浏览器策略阻止**；现已修复，错误页还会主动展示已累积的 dsh 日志与排查建议

---

## 🚀 安装 / 校验

- 安装包：`DeepSeek-Harness-Setup-1.4.5.exe`（约 148.8 MB）
- 国内下载（推荐）：https://cnb.cool/cnskycn/deepseek-harness-desktop/-/releases/download/latest/DeepSeek-Harness-Setup-1.4.5.exe
- 首次启动或升级后需初始化运行时，通常 30–90 秒；启动页会显示等待进度

## ⬆️ 自动更新

- 主通道：CNB 国内 CDN（`https://cnb.cool/cnskycn/deepseek-harness-desktop/-/releases/download/latest/`）
- 备用通道：GitHub Releases（主通道不可用时自动回退）

> ⚠️ **1.4.4 用户注意**：由于 1.4.4 可能完全无法打开界面，若自动更新不可用，请直接下载本版本安装包覆盖安装。

---

Apache-2.0 © DeepSeek Harness Desktop contributors
